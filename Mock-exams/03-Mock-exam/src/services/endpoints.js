let baseUrl = 'http://localhost:3030';
let registerUrl = `${baseUrl}/users/register`;
let loginUrl = `${baseUrl}/users/login`;
let logoutUrl = `${baseUrl}/users/logout`;

let itemsUrl = `${baseUrl}/data/books`;
let likesUrl = `${baseUrl}/data/likes`;

export default {baseUrl, registerUrl, loginUrl, logoutUrl, itemsUrl, likesUrl};